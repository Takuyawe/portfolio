---
import { Icon } from 'astro-icon/components'
---

<div id="about" class="relative h-screen w-full flex items-center justify-center">
  <div class="flex flex-col w-2/5 gap-y-5">
    <p class="text-white text-6xl leading-tight">
      Hi, I'm Takuya <br />
      Web Developer
    </p>
    <p class="text-white opacity-50 text-xl">Front-End / Web Application Developer</p>
  </div>
  <div class="w-2/5 text-white leading-6 space-y-4">
    <p>Front-end engineer with a passion for building user-friendly and scalable web applications.</p>
    <p>
      Experienced in collaborating with designers and backend developers to deliver fast, maintainable UIs using React,
      TypeScript, and Next.js.
    </p>
    <p>
      Always curious and eager to improve—whether it's optimizing performance, learning new tech, or exploring UX best
      practices.
    </p>
    <p>
      Outside of work, I enjoy playing soccer, watching anime, and diving into new ideas through coding side projects.
    </p>
  </div>
  <div
    id="about-move-to-bottom"
    class="hidden md:flex items-center gap-x-1 absolute bottom-12 right-6 text-white hover:text-gray-300 cursor-pointer text-sm [writing-mode:vertical-lr]"
  >
    Scroll to Bottom <Icon class="inline" name="down-arrow" />
  </div>
</div>

<script>
  import { PAGE_ID } from '../consts'
  import { getViewSectionId } from '../modules'
  import { currentPageId } from '../stores/pageId'

  document.getElementById('about-move-to-bottom')?.addEventListener('click', (e) => {
    e.preventDefault()
    currentPageId.set(PAGE_ID.CONTACT)
    const sectionId = getViewSectionId(PAGE_ID.CONTACT)
    document.getElementById(sectionId)?.scrollIntoView({ behavior: 'smooth' })
  })
</script>
